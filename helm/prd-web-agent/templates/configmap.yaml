apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "prd-web-agent.fullname" . }}
  labels:
    {{- include "prd-web-agent.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
